version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - cd CS464_Experiment
        - npx install expo
        - >
          if [ -f yarn.lock ]; then
            yarn
          elif [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
            npx i
          else
            npx install
          fi
    build:
      commands:
        - npx expo build:web
  artifacts:
    baseDirectory: web-build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - $(npx root --global)/**/*
